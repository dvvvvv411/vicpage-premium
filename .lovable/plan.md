

# Logo groesser skalieren

## Aenderungen

Nur die CSS-Klasse der Logo-`<img>`-Tags wird angepasst -- von `h-8` auf `h-12`. Header-Hoehe und Footer-Layout bleiben unveraendert.

| Datei | Aenderung |
|-------|-----------|
| `src/components/Navbar.tsx` | `className="h-8 w-auto"` -> `className="h-12 w-auto"` |
| `src/components/Footer.tsx` | `className="h-8 w-auto"` -> `className="h-12 w-auto"` |

Keine weiteren Aenderungen noetig. Das Bild wird einfach groesser gerendert bei voller Qualitaet.

